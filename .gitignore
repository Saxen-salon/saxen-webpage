# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# sales / outreach (private, per-project — generated by /sales-brief)
SALES_BRIEF.md

# setup-website.sh state (generated per-project, contains local config)
.setup-state
.setup-copy-done

# browser-qa screenshots — regenerated each run, binaries we don't want to track
# (the rest of .redesign-state/ stays committed for pipeline resumption)
.redesign-state/screenshots/
